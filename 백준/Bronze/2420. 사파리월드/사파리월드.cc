#include <bits/stdc++.h>
using namespace std;
using ll = long long;

ll n, m;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> n >> m;
    cout << abs(n-m);

    return 0;
}